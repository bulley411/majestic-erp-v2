# server {
#   listen 80;
#   server_name _;
#   root /usr/share/nginx/html;
#   index index.html;

#   # Without this, everything is served as the default type and the browser
#   # refuses to execute JavaScript or apply CSS.
#   include /etc/nginx/mime.types;
#   default_type application/octet-stream;

#   gzip on;
#   gzip_types text/css application/javascript application/json image/svg+xml;

#   # Hashed asset filenames are immutable; index.html must never be cached.
#   location /assets/ {
#     expires 1y;
#     add_header Cache-Control "public, immutable";
#   }

#   location / {
#     try_files $uri $uri/ /index.html;
#     add_header Cache-Control "no-cache";
#   }
# }



server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  include /etc/nginx/mime.types;
  default_type application/octet-stream;

  gzip on;
  gzip_types text/css application/javascript application/json image/svg+xml;

  # Hashed asset filenames are immutable; index.html must never be cached.
  location /assets/ {
    expires 1y;
    add_header Cache-Control "public, immutable";
  }

  # Proxy API calls to the API container
  location /api/ {
    proxy_pass http://api:3000/api/;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_read_timeout 300s;
    proxy_connect_timeout 75s;
  }

  location / {
    try_files $uri $uri/ /index.html;
    add_header Cache-Control "no-cache";
  }
}